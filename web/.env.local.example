# Copy to web/.env.local for local dev. The Next.js app uses these to find the FastAPI server.
#
# In production we'd flip these to whatever URL the backend runs at; for local dev,
# leave them as the defaults.

# Where the FastAPI server (scripts/api.py) is listening.
AUGUR_API_BASE=http://localhost:8000
